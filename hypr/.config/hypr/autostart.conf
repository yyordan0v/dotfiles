# Extra autostart processes
# exec-once = uwsm app -- my-service

# Signal UWSM that compositor is ready, activating graphical-session.target
# (required for systemd user services like voxtype to auto-start)
exec-once = uwsm finalize
